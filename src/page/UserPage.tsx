import React from 'react';

const UserPage: React.FC = () => {

    return (
        <div>
            <button type="button">
                메인으로
            </button>
        </div>
    );
};

export default UserPage;
